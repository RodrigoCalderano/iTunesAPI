<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ciandt.book.seeker">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".BooksApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcherv2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcherv2"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.BookSearchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.BookDetailsView" />
        <activity android:name=".view.BookListView"/>

    </application>

</manifest>